@{
    ViewData["Title"] = "Erişim Reddedildi";
    Layout = "_Layout";
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erişim Reddedildi | 403 Hatası</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            color: #343a40;
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 650px;
            width: 100%;
            text-align: center;
            padding: 40px;
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        .container:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(to right, #dc3545, #ff7b7b);
        }

        .icon {
            font-size: 90px;
            color: #dc3545;
            margin-bottom: 25px;
            animation: pulse 1.5s infinite alternate;
        }

        @@keyframes pulse {
            from { transform: scale(1); }
            to { transform: scale(1.05); }
        }

        h1 {
            color: #dc3545;
            margin-bottom: 20px;
            font-size: 32px;
            font-weight: 700;
        }

        p {
            margin-bottom: 20px;
            font-size: 18px;
            color: #6c757d;
            line-height: 1.6;
        }

        .actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            padding: 14px 28px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn i {
            margin-right: 8px;
        }

        .btn-primary {
            background-color: #007bff;
            color: white;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0069d9;
            transform: translateY(-3px);
            box-shadow: 0 6px 10px rgba(0, 123, 255, 0.2);
        }

        .btn-secondary {
            background-color: transparent;
            color: #6c757d;
            border: 2px solid #6c757d;
        }

        .btn-secondary:hover {
            background-color: #f8f9fa;
            transform: translateY(-3px);
            color: #495057;
            border-color: #495057;
        }

        .error-code {
            margin-top: 30px;
            padding: 12px;
            font-size: 14px;
            color: #adb5bd;
            background-color: #f8f9fa;
            border-radius: 6px;
            display: inline-block;
        }

        .help-text {
            margin-top: 25px;
            padding: 15px;
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
            border-radius: 4px;
            text-align: left;
        }

        /* Medya sorgusu düzgün bir şekilde yazıldı */
        @@media (max-width: 768px) {
            .container {
                padding: 30px 20px;
            }

            .icon {
                font-size: 70px;
            }

            h1 {
                font-size: 28px;
            }

            p {
                font-size: 16px;
            }

            .actions {
                flex-direction: column;
                gap: 12px;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }
        }

        @@media (max-width: 480px) {
            .container {
                padding: 25px 15px;
            }

            .icon {
                font-size: 60px;
            }

            h1 {
                font-size: 24px;
            }

            p {
                font-size: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">
            <i class="fas fa-ban"></i>
        </div>
        <h1>Erişim Reddedildi</h1>
        <p>Maalesef bu sayfaya erişim izniniz bulunmamaktadır. Bu kaynağı görüntülemek için gerekli yetkilere sahip değilsiniz.</p>

        <div class="help-text">
            <p><i class="fas fa-info-circle"></i> <strong>Yardım:</strong> Eğer bu bir hata olduğunu düşünüyorsanız, lütfen sistem yöneticinizle iletişime geçin veya hesabınızda yetki değişikliği talep edin.</p>
        </div>

        <div class="actions">
            <a href="/" class="btn btn-primary">
                <i class="fas fa-home"></i> Ana Sayfaya Dön
            </a>
            <a href="javascript:history.back()" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Önceki Sayfaya Dön
            </a>
            <a href="/contact" class="btn btn-secondary">
                <i class="fas fa-envelope"></i> İletişime Geç
            </a>
        </div>

        <div class="error-code">
            <i class="fas fa-code"></i> Hata Kodu: 403 - Forbidden
        </div>
    </div>

    <script>
        // Küçük bir etkileşim için JavaScript
        document.addEventListener('DOMContentLoaded', function () {
            const container = document.querySelector('.container');
            container.style.opacity = 0;

            let opacity = 0;
            const fadeIn = setInterval(() => {
                if (opacity < 1) {
                    opacity += 0.03;
                    container.style.opacity = opacity;
                } else {
                    clearInterval(fadeIn);
                }
            }, 20);
        });
    </script>
</body>
</html>